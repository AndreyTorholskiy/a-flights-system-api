api:
  version: "v1"
  
http:
  listener:
    path: "/a-flights-sa/api/${api.version}"
     
db:
  mysql:
    host: "mudb.mule-training.com"
    port: "3306"
    user: "mule"
    password: "mule"
    database: "training"
